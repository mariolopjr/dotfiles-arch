# List of useful plugins.
# Commented out (#) plugins are not in use yet.

plugins:
  [
    #absubmit,
    acousticbrainz,
    badfiles,
    bandcamp,
    # bucket,
    chroma,
    check,
    convert,
    copyartifacts,
    duplicates,
    edit,
    embedart,
    export,
    fetchart,
    # follow,
    fromfilename,
    hook,
    importadded,
    info,
    lastimport,
    lastgenre,
    lyrics,
    # mbcollection,
    # mbsubmit,
    mbsync,
    missing,
    parentwork,
    permissions,
    replaygain,
    unimported
  ]